# Build-time URL of the Go backend (required for Timeweb Frontend / static deploy).
# Vite embeds this into the bundle during `npm run build`.
VITE_API_BASE_URL=https://cybermatebot-app-back-0d85.twc1.net
